name: Pack Release

on:
  push:
    branches: [master]
    paths:
      - 'packs/**'
<<<<<<< HEAD
      - '!packs/**/README.md'
=======
>>>>>>> 8ed39bb277d772ccadb14d4ad92771110aa8c765

permissions:
  contents: read

jobs:
  upload-packs:
    runs-on: ubuntu-latest
    env:
      RADEGAST_API_KEY: ${{ secrets.RADEGAST_KEY }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - uses: astral-sh/setup-uv@v5

      - name: Install dependencies
        run: uv sync

      - name: Find changed packs
        id: changed
        run: |
          PACKS=$(git diff --name-only "${{ github.event.before }}" "${{ github.sha }}" -- packs/ \
<<<<<<< HEAD
            | grep -v 'README\.md' \
=======
>>>>>>> 8ed39bb277d772ccadb14d4ad92771110aa8c765
            | grep -oP 'packs/\K[^/]+/[^/]+' \
            | sort -u)

          echo "packs<<EOF" >> "$GITHUB_OUTPUT"
          echo "$PACKS" >> "$GITHUB_OUTPUT"
          echo "EOF" >> "$GITHUB_OUTPUT"

<<<<<<< HEAD
      - name: Build and upload changed packs
=======
      - name: Upload changed packs to Radegast
>>>>>>> 8ed39bb277d772ccadb14d4ad92771110aa8c765
        run: |
          echo "## Pack Release Results" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"

          FAILED=0
          while IFS= read -r pack; do
            [ -z "$pack" ] && continue
<<<<<<< HEAD

            echo "Building pack.yml for $pack..."
            if ! uv run python tools/build_packs.py --pack "$pack"; then
              echo "- ❌ \`$pack\` (build_packs failed)" >> "$GITHUB_STEP_SUMMARY"
              FAILED=1
              continue
            fi

=======
>>>>>>> 8ed39bb277d772ccadb14d4ad92771110aa8c765
            echo "Uploading $pack..."
            if uv run python tools/upload_radegast.py --pack "$pack"; then
              echo "- ✅ \`$pack\`" >> "$GITHUB_STEP_SUMMARY"
            else
              echo "- ❌ \`$pack\` (upload failed)" >> "$GITHUB_STEP_SUMMARY"
              FAILED=1
            fi
          done <<< "${{ steps.changed.outputs.packs }}"

          exit $FAILED
